import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.autos.app',
  appName: 'admin-autos-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
